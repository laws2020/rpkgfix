% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_namespace.R
\name{add_namespaces}
\alias{add_namespaces}
\title{Prefix Bare Function Calls with Package Namespaces}
\usage{
add_namespaces(dir = "R", mapping = default_mapping(), dry_run = FALSE)
}
\arguments{
\item{dir}{Character. Directory containing \code{.R} files. Defaults to \code{"R"}.}

\item{mapping}{Named list mapping package names to character vectors of
function names. Defaults to \code{\link[=default_mapping]{default_mapping()}}.}

\item{dry_run}{Logical. If \code{TRUE}, reports what would change without writing
any files. Defaults to \code{FALSE}.}
}
\value{
Invisibly, a list with \code{ok}, \code{errors}, \code{warnings}, \code{notes}, plus a
\code{changes} element: a named list of the substitutions made per file.
}
\description{
Adds \verb{pkg::} prefixes to bare function calls across all \code{.R} files in a
directory, using a lookup table (see \code{\link[=default_mapping]{default_mapping()}}). Safe to run
repeatedly - it will not double-prefix calls that already carry a namespace.
Renamed from the old \code{prefix_all()}.
}
\details{
The substitution uses a Perl lookbehind so a call is only prefixed when it is
\emph{not} already namespaced (\verb{(?<![A-Za-z0-9_]::)}) and \emph{not} part of a dotted
name (\verb{(?<!\\.)}). Files are read and written as UTF-8.
}
\examples{
\dontrun{
add_namespaces()
add_namespaces(dry_run = TRUE)
}
}
