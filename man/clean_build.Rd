% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_build.R
\name{clean_build}
\alias{clean_build}
\title{Remove Stale Build Artefacts from the Package Directory}
\usage{
clean_build(
  rdata = TRUE,
  rhistory = TRUE,
  bundles = TRUE,
  orphan_rd = TRUE,
  dry_run = FALSE
)
}
\arguments{
\item{rdata}{Logical. Remove \code{.RData}. Default \code{TRUE}.}

\item{rhistory}{Logical. Remove \code{.Rhistory}. Default \code{TRUE}.}

\item{bundles}{Logical. Remove \verb{*.tar.gz} and \verb{*.Rcheck}. Default \code{TRUE}.}

\item{orphan_rd}{Logical. Remove orphaned \verb{man/*.Rd}. Default \code{TRUE}.}

\item{dry_run}{Logical. If \code{TRUE}, lists what would be removed without
deleting. Default \code{FALSE}.}
}
\value{
Invisibly, a list with \code{ok}, \code{errors}, \code{warnings}, \code{notes}, plus a
\code{deleted} element (character vector of paths).
}
\description{
Removes development debris that should never be committed or shipped:
\code{.RData}, \code{.Rhistory}, \verb{*.tar.gz} bundles, \verb{*.Rcheck} directories, and
orphaned \verb{man/*.Rd} files (those with no matching function in \verb{R/}).
Renamed from the old \code{clean_session()} (it cleans build artefacts, not the R
session).
}
\examples{
\dontrun{
clean_build(dry_run = TRUE)
clean_build()
}
}
