% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bump_version.R
\name{bump_version}
\alias{bump_version}
\title{Bump the Package Version and Add a NEWS Entry}
\usage{
bump_version(
  which = c("patch", "minor", "major"),
  path = ".",
  update_news = TRUE,
  dry_run = FALSE
)
}
\arguments{
\item{which}{Character. One of \code{"patch"}, \code{"minor"}, or \code{"major"}.
Defaults to \code{"patch"}.}

\item{path}{Character. Package root. Defaults to \code{"."}.}

\item{update_news}{Logical. If \code{TRUE} (default), prepends a heading to
\code{NEWS.md} (creating it if absent).}

\item{dry_run}{Logical. If \code{TRUE}, reports the new version without writing.
Defaults to \code{FALSE}.}
}
\value{
Invisibly, a list with \code{ok}, \code{errors}, \code{warnings}, \code{notes}, plus
\code{old_version} and \code{new_version}.
}
\description{
Increments the \code{Version} field in \code{DESCRIPTION} (\code{patch}, \code{minor}, or
\code{major}) and prepends a matching heading to \code{NEWS.md}. Pure base R - no
dependency on \code{usethis}, so it works non-interactively (e.g. in CI).
Renamed from the old \code{bump()}.
}
\examples{
\dontrun{
bump_version()          # patch: 0.1.0 -> 0.1.1
bump_version("minor")   # 0.1.1 -> 0.2.0
bump_version("major")   # 0.2.0 -> 1.0.0
}
}
