% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_all.R
\name{check_all}
\alias{check_all}
\title{Run the Full rpkgfix Diagnostic Suite}
\usage{
check_all(
  dir = "R",
  path = ".",
  checks = c("check_description", "check_license", "check_imports", "check_docs",
    "check_namespace", "check_encoding", "check_style", "check_examples",
    "check_coverage", "check_spelling", "check_output", "check_side_effects",
    "check_news")
)
}
\arguments{
\item{dir}{Character. Directory containing \code{.R} source files. Defaults to
\code{"R"}.}

\item{path}{Character. Package root. Defaults to \code{"."}.}

\item{checks}{Character vector. Which diagnostics to run. Defaults to all
fifteen \verb{check_*} functions. Pass a subset to run only those.}
}
\value{
Invisibly, a list with \code{ok}, \code{errors}, \code{warnings}, \code{notes}, plus a
named \code{results} element holding each individual check's return value.
}
\description{
Runs every read-only \verb{check_*()} diagnostic in one pass and prints a
consolidated summary of total errors, warnings, and notes. Ideal as a
pre-commit / pre-push step or a first look before \code{devtools::check()}.
}
\details{
Each diagnostic returns \code{list(ok, errors, warnings, notes)}; \code{check_all()}
concatenates those vectors across all checks so the top-level \code{ok} is \code{TRUE}
only when no check reported an error. \code{check_deps()} and \code{check_globals()}
are excluded from the default set because they need a package \emph{name} /
loadable package rather than a source directory - run them directly.
}
\examples{
\dontrun{
check_all()
res <- check_all()
res$results$check_description$errors
}
}
