% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_output.R
\name{check_output}
\alias{check_output}
\title{Detect Console Output in Package Code}
\usage{
check_output(dir = "R")
}
\arguments{
\item{dir}{Character. Directory containing R source. Defaults to \code{"R"}.}
}
\value{
Invisibly, a list with \code{ok}, \code{errors}, \code{warnings}, \code{notes}.
}
\description{
Package code should emit diagnostics via \code{message()}/\code{warning()} (or a
messaging framework) so users can suppress them - not via \code{cat()} or
\code{print()}. This flags \code{cat()}/\code{print()} calls in \verb{R/}. Comments and string
literals are stripped first, so the detection regex itself and docstrings
are not matched.
}
\examples{
\dontrun{
check_output()
}
}
