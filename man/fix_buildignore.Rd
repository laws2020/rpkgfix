% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fix_buildignore.R
\name{fix_buildignore}
\alias{fix_buildignore}
\title{Populate .Rbuildignore With Non-Standard Files and Folders}
\usage{
fix_buildignore(
  extra = character(0),
  standard_folders = c("R", "man", "data", "data-raw", "inst", "tests", "vignettes",
    "src", "po", "exec", "tools", "demo"),
  dry_run = FALSE
)
}
\arguments{
\item{extra}{Character vector of additional patterns to always add.
Defaults to \code{character(0)}.}

\item{standard_folders}{Character vector of folders that are part of the
standard package structure and should not be ignored.}

\item{dry_run}{Logical. If \code{TRUE}, reports what would be added without
writing. Defaults to \code{FALSE}.}
}
\value{
Invisibly, a list with \code{ok}, \code{errors}, \code{warnings}, \code{notes}, plus an
\code{added} element (character vector of patterns written).
}
\description{
Scans the package root for non-standard folders \emph{and} common stray files
(e.g. \code{cran-comments.md}, \verb{*.Rproj}, \code{.github}) and adds correctly anchored
regex patterns to \code{.Rbuildignore}. Safe to run repeatedly - existing
patterns are not duplicated. Renamed from the old \code{ignore()}.
}
\examples{
\dontrun{
fix_buildignore()
fix_buildignore(dry_run = TRUE)
}
}
