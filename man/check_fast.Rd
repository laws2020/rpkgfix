% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_fast.R
\name{check_fast}
\alias{check_fast}
\title{Fast Namespace and Global Variable check_fast(}
\usage{
check_fast(pkg = ".", quiet = TRUE)
}
\arguments{
\item{pkg}{Character. Path to the package root. Defaults to \code{"."} (current
directory).}

\item{quiet}{Logical. If \code{TRUE}, suppresses \code{devtools::check()} output and
only prints the rpkgfix summary. Defaults to \code{TRUE}.}
}
\value{
Invisibly, a list with \code{ok}, \code{errors}, \code{warnings}, \code{notes}, plus a
\code{result} element holding the object returned by \code{devtools::check()}.
}
\description{
Runs a targeted \verb{R CMD check} pass that focuses only on namespace problems
and undefined global variables - skipping examples, tests, vignettes, and
manual pages to give a quick feedback loop during development.
}
\details{
This function requires \code{devtools}, a \code{Suggests} dependency. It is
intentionally in \code{Suggests} rather than \code{Imports} because \code{devtools} is a
development tool and should not be a runtime dependency; the check is guarded
with \code{requireNamespace()}.

The flags passed to \verb{R CMD check} are:
\itemize{
\item \code{--no-codoc}: skip code/documentation consistency
\item \code{--no-examples}: skip example execution
\item \code{--no-tests}: skip test suite
\item \code{--no-manual}: skip PDF manual build
\item \code{--no-vignettes}: skip vignette build
}
}
\examples{
\dontrun{
check_fast()
check_fast((quiet = FALSE)  # show full devtools output
}
}
